#include <iostream> 
#include <vector>

using std::cout;
using std::endl;
using std::vector;

int main()
{
	vector<int> myVector={4,7,3,2,1};
	vector<int> myVector2={3,5};
	vector<int> myVector3={5};
	vector<int> myVector4;

	cout << "MyVector3 is of size: " << vector4.size() << endl;

	vector<float> R(20,5); 
	vecotr<vector<float>> N (5,R);
	
	cout << " N contains: " << endl;
	
	for (auto Row : N)
	{
	}
	cout << "myVector *now* contains: " << endl;
	for (auto i: myVector)
	{
		cout << i << endl;
	}
}
